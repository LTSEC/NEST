all:
  vars:
    ansible_user: root
    ansible_password: changeme
    ansible_ssh_private_key_file: ~/.ssh/id_rsa

  children:
    webservers:
      hosts:
        1.2.3.4:
        1.2.3.5:
      vars:
        http_port: 80