package services

import (
	"bytes"
	"fmt"
	"io"
	"os"
	"sync"
	"time"

	"github.com/LTSEC/NEST/enum"
	"github.com/jlaffaye/ftp"
	"golang.org/x/exp/rand"
)

// establishSQLConnection is a utility function that attempts to connect to the designated ip
// address at the designated port with the FTP protocol, and returns the connection or an error.
func establishDBconnection(service enum.Service, address string) (int, bool, error) {

}

func ScoreSQLLogin(service enum.Service, address string) (int, bool, error) {
}

func ScoreSQLRead(service enum.Service, address string) (int, bool, error) {
}

func ScoreSQLWrite(service enum.Service, address string) (int, bool, error) {
}

func ScoreSQLSchema(service enum.Service, address string) (int, bool, error) {
}
